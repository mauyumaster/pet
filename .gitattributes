# 行尾纪律 —— 这不是风格问题，是可执行性/一致性问题。
#
# 起因（2026-09-21）：pack-release.cmd 用 `>` / `>>` 生成 version.json，
# cmd.exe 写出来是 CRLF，而 git 里存的是 LF ⇒ 每次打包后 version.json
# 都显示为「已修改」，实际内容一字未变。这种假改动会掩盖真改动。
#
# ⚠ .cmd / .bat 必须是 CRLF：cmd.exe 解析批处理对行尾敏感，LF-only 会在
#   goto / 标签 / 多行括号块上**静默半途失效**（本项目踩过：start /b 那行没执行，
#   末尾开浏览器那行执行了 ⇒ 双击后 ERR_CONNECTION_REFUSED，全程零报错）。
#   所以这两个扩展名显式锁死 CRLF，不让 git 自作主张。
*.cmd text eol=crlf
*.bat text eol=crlf

# C# / markdown：仓库内统一 LF（写盘如何不管，git 里一致即可）。
*.cs   text eol=lf
*.md   text eol=lf
*.json text eol=lf
*.csproj text eol=lf

# 二进制：不许碰。
*.exe  binary
*.ico  binary
*.glb  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.zip  binary
