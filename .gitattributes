# ⚠ 本文件自身也必须锁 LF：core.autocrlf=true 下缺这条时，git 每次 checkout 都想把它
#   转成 CRLF ⇒ 它**永远显示为已修改** —— 正是这个文件一开头要治的那种病，先治自己。
/.gitattributes text eol=lf

# 行尾纪律 —— 这不是风格问题，是可执行性/一致性问题。
#
# 起因（2026-09-21）：pack-release.cmd 用 `>` / `>>` 生成 version.json，
# cmd.exe 写出来是 CRLF，而 git 里存的是 LF ⇒ 每次打包后 version.json
# 都显示为「已修改」，实际内容一字未变。这种假改动会掩盖真改动。
#
# ⚠ .cmd / .bat 必须是 CRLF：cmd.exe 解析批处理对行尾敏感，LF-only 会在
#   goto / 标签 / 多行括号块上**静默半途失效**（本项目踩过：start /b 那行没执行，
#   末尾开浏览器那行执行了 ⇒ 双击后 ERR_CONNECTION_REFUSED，全程零报错）。
#   所以这两个扩展名显式锁死 CRLF，不让 git 自作主张。
*.cmd text eol=crlf
*.bat text eol=crlf

# C# / markdown：仓库内统一 LF（写盘如何不管，git 里一致即可）。
*.cs   text eol=lf
*.md   text eol=lf
*.json text eol=lf
*.csproj text eol=lf

# README 配图与自检脚本（2026-09-21 加）。
# ⚠ *.svg 也必须锁 LF —— 它同时被两个 Python 判据按「文本」读并正则匹配，
#   行尾不统一会让「同一份文件两个落点」的老毛病在判据侧复现。
*.svg  text eol=lf
*.py   text eol=lf

# 注入页面的钩子脚本，靠 tools/hook_check.js 用 node 真跑一遍（2026-09-25 加）。
# ⚠ 这是仓库里**唯一被程序读取并执行**的 js，与 .cmd 属于同一类风险：行尾在仓库与工作区
#   之间来回转换只会制造假改动（见本文件开头 version.json 那条）。core.autocrlf=true 下
#   缺这条规则时 git 每次 checkout 都想把它改成 CRLF ⇒ 它**永远显示为已修改**。
tools/*.js text eol=lf

# 二进制：不许碰。
*.exe  binary
*.ico  binary
*.glb  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.zip  binary
