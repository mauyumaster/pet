# 构建产物
bin/
obj/
release/

# 发布包（由 pack-release.cmd 生成）
# ⚠ 26MB 的二进制属于 Release 附件，不属于 git 历史 —— 每次发布都提交一份
#   会让仓库快速膨胀，而且二进制 diff 毫无信息量。挂到 GitHub Release 页即可。
AzhuPet-v*.zip
# 打包事故的存证文件（2026-09-26：GNU tar 静默产出的 40MB 假 zip），
# 以及任何 AzhuPet-v*.zip 的派生/备份，一律不进 git。见 pack-release.cmd [6/8]。
AzhuPet-v*.zip.*

# ⚠⚠ 本目录曾存在 _spike_tmp/（126MB，含私钥）。它必须永远不进 git。
_spike_tmp/
_bak_*/
_report/

# 测试/诊断产物（下划线开头 = 可随时删除的东西）
_desktop_check*
_icon_*
_verify_*
_tmp_*
# 当年抓 Trae 接口的探针脚本 —— 教人抓凭据的脚本不适合公开发布
trae_chat_spike.ps1

# IDE / 系统
*.user
.vs/
Thumbs.db
